# AI 뉴스 콜렉터 PRD

## 1. 개요
해외 AI 뉴스를 RSS로 수집하여 한국어로 번역/요약한 뒤 카드 형태로 보여주는 개인용 웹 앱.
클로드 AI가 번역/요약과 카테고리 분류를 담당하며, 관심 있는 뉴스는 데이터베이스와 Notion에 저장할 수 있다.

## 2. 목표 사용자
- 단일 사용자 (개인 사용, 멀티 유저 고려 없음)

## 3. 기술 스택
| 영역 | 기술 |
|---|---|
| 프레임워크 | Next.js |
| 로그인 | Clerk |
| 데이터베이스 | Neon (PostgreSQL) |
| ORM | Drizzle ORM |
| 번역/요약/카테고리 분류 | 클로드 API |
| 외부 저장 | Notion (MCP 서버) |

## 4. 뉴스 소스 (RSS)
- TechCrunch AI: `https://techcrunch.com/category/artificial-intelligence/feed/`
- The Verge AI: `https://www.theverge.com/rss/ai-artificial-intelligence/index.xml`
- VentureBeat AI: `https://venturebeat.com/category/ai`

## 5. 핵심 기능

### 5.1 로그인
- Clerk를 통한 로그인 필요. 로그인 전에는 앱의 어떤 화면도 접근 불가.

### 5.2 뉴스 가져오기
- "뉴스 가져오기" 버튼 클릭 시 등록된 RSS 소스에서 최신 뉴스를 수집.
- 수집된 뉴스는 즉시 DB에 저장되지 않고 화면에만 표시됨 (임시 상태).
- **중복 처리**: 원문 URL을 기준으로 이미 가져온 적 있는 뉴스는 제외하고 표시.

### 5.3 번역/요약 및 카테고리 분류
- 클로드 API가 각 뉴스 기사를 한국어로 번역/요약.
- 클로드 API가 기사 내용을 분석해 카테고리를 자유롭게 제안(고정 목록 아님).
- 제안된 카테고리가 기존 카테고리 목록에 없으면 자동으로 카테고리 목록에 추가됨.

### 5.4 카테고리 관리
- 카테고리별 필터링으로 원하는 뉴스만 조회 가능.
- 카테고리 추가/삭제/수정(CRUD) 가능 (클로드가 자동 생성한 카테고리 포함).

### 5.5 뉴스 카드
각 뉴스 카드에는 다음 요소를 포함:
- 한국어 번역/요약 내용
- 분류된 카테고리
- **원문보기** 링크 — 클릭 시 원문 기사로 이동
- **저장하기** 버튼 — 클릭 시 DB에 저장, 저장 후 버튼이 "저장됨"으로 전환(비활성화)
- **Notion 저장** 버튼 — DB 저장 여부와 무관하게 독립적으로 클릭 가능, 클릭 시 Notion MCP를 통해 저장되고 버튼이 "Notion 저장됨"으로 전환

## 6. 데이터 처리 규칙
- **중복 판단 기준**: 원문 URL
- **저장 시점**: "가져오기" 시점이 아닌, 사용자가 "저장하기" 버튼을 클릭한 시점에 DB 저장
- **카테고리 자동 추가**: 클로드가 제안한 카테고리가 기존 목록에 없으면 자동 생성
- **DB 저장과 Notion 저장은 서로 독립적**: 한쪽 저장 여부가 다른 쪽에 영향을 주지 않음

## 7. 화면 상태 요약
| 액션 | 결과 |
|---|---|
| 뉴스 가져오기 클릭 | RSS 수집 → 번역/요약/분류 → 중복 제외 → 화면에 카드로 표시 |
| 저장하기 클릭 | DB에 저장 → 버튼 "저장됨"으로 전환 |
| Notion 저장 클릭 | Notion에 저장 → 버튼 "Notion 저장됨"으로 전환 |
| 원문보기 클릭 | 새 탭/페이지로 원문 기사 이동 |
| 카테고리 필터 선택 | 해당 카테고리 뉴스만 표시 |
| 카테고리 추가/삭제/수정 | 카테고리 목록 갱신 |

## 8. 향후 논의 필요 사항 (미확정)
- RSS 소스 자체의 추가/삭제 가능 여부
- 저장된 뉴스 목록을 별도로 조회하는 화면 필요 여부
- 카테고리 삭제 시 해당 카테고리로 분류된 기존 뉴스 처리 방식
